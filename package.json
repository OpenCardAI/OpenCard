{
  "name": "@opencard/sdk",
  "version": "0.0.1",
  "description": "Universal AI wallet SDK with React components",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --outfile=dist/index.mjs --external:react --external:react-dom && esbuild src/index.js --bundle --format=cjs --outfile=dist/index.js --external:react --external:react-dom",
    "dev": "npm run build -- --watch",
    "clean": "rm -rf dist",
    "demo:vanilla": "cd demo/vanilla-js && (python3 -m http.server 8080 > /dev/null 2>&1 & echo $! > .server.pid) && sleep 1 && open http://localhost:8080 || (cd demo/vanilla-js && python -m SimpleHTTPServer 8080 > /dev/null 2>&1 & echo $! > .server.pid && sleep 1 && xdg-open http://localhost:8080)",
    "demo:react": "cd demo/react && npm install && npm run dev",
    "demo": "echo '📦 Available demos:\n  npm run demo:vanilla  - Vanilla JS demo\n  npm run demo:react    - React demo\n\nSee demo/README.md for more info'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/opencard/opencard.git"
  },
  "keywords": [
    "opencard",
    "ai",
    "wallet",
    "react",
    "sdk"
  ],
  "author": "OpenCard",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "openai": "^4.77.0"
  },
  "peerDependencies": {
    "react": ">=16.8.0",
    "react-dom": ">=16.8.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  }
}